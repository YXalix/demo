using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class lkcontroller : MonoBehaviour
{
    public int nowindex;
    // Start is called before the first frame update
    void Start()
    {
        nowindex = 0;

    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
